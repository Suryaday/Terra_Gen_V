Known Issues

1. BM25 appears to load twice during startup
2. Cross-encoder reranking takes ~2 minutes/query on CPU
3. Memory drops significantly during benchmark execution
4. Benchmark suite more stable in batches of 5
5. Retrieval candidate count may be too high